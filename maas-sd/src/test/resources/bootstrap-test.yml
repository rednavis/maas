server:
  port: 8761

spring:
  application:
    name: test
  cloud:
    #Disable discovery
    #    discovery:
    #      enabled: false
    #Disable cloud config and config discovery
    config:
      discovery:
        enabled: false
      enabled: false
    service-registry:
      auto-registration:
        enabled: false