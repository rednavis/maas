spring:
  application:
    name: maas-data
  cloud:
    config:
      uri: http://localhost:8088
  data:
    mongodb:
      database: maas
      host: localhost
      port: 27017
      auto-index-creation: true